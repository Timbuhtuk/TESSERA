<UserControl x:Class="DomainColorTest.EditorMaterialPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Foreground="#EEEEEE">
    <UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="Theme.xaml"/></ResourceDictionary.MergedDictionaries>
        <DrawingBrush x:Key="MaterialChecker" TileMode="Tile" Viewport="0,0,16,16" ViewportUnits="Absolute">
            <DrawingBrush.Drawing><DrawingGroup>
                <GeometryDrawing Brush="#191919" Geometry="M0,0 H16 V16 H0 Z"/>
                <GeometryDrawing Brush="#303030" Geometry="M0,0 H8 V8 H0 Z M8,8 H16 V16 H8 Z"/>
            </DrawingGroup></DrawingBrush.Drawing>
        </DrawingBrush>
    </ResourceDictionary></UserControl.Resources>
    <Grid>
        <Button x:Name="materialTrigger" Content="Из редактора  ▾" Padding="18,11" AutomationProperties.Name="Выбрать материал из редактора"/>
        <Popup x:Name="materialPopup" PlacementTarget="{Binding ElementName=materialTrigger}" Placement="Bottom" VerticalOffset="4" StaysOpen="False" AllowsTransparency="True">
            <Border x:Name="materialPanel" Background="#191919" BorderBrush="#686868" BorderThickness="1" Padding="12" MaxWidth="430">
                <ScrollViewer MaxHeight="350" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock Text="Исходники" FontSize="14" FontWeight="SemiBold" Margin="2,0,0,8"/>
                        <WrapPanel x:Name="materialSources"/>
                        <TextBlock x:Name="materialResultsTitle" Text="Результаты" FontSize="14" FontWeight="SemiBold" Margin="2,12,0,8"/>
                        <WrapPanel x:Name="materialResults"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
