<UserControl x:Class="DomainColorTest.AsepriteWorkspace"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DomainColorTest"
    Background="{DynamicResource CanvasBrush}" Foreground="{DynamicResource TextBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries><ResourceDictionary Source="Theme.xaml"/></ResourceDictionary.MergedDictionaries>
            <Style x:Key="SheetItemStyle" TargetType="ListBoxItem">
                <Setter Property="Margin" Value="0,0,8,0"/><Setter Property="Padding" Value="5"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Frame" Background="{StaticResource RaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource TextBrush}"/><Setter TargetName="Frame" Property="Background" Value="{StaticResource HoverBrush}"/></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="{StaticResource TextMutedBrush}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value></Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="sheetOpenButton" Content="Открыть файлы…" Margin="0,0,7,0" ToolTip="Открыть несколько .aseprite или .ase (Ctrl+O)"/>
                    <Button x:Name="sheetSaveButton" Content="Сохранить выбранный…" Margin="0,0,7,0" IsEnabled="False"/>
                    <Button x:Name="sheetSaveAllButton" Content="Сохранить все…" Style="{StaticResource PrimaryButton}" Margin="0,0,7,0" IsEnabled="False"/>
                    <Button x:Name="sheetCancelButton" Content="Отменить" Visibility="Collapsed"/>
                </StackPanel>
                <Button x:Name="sheetBackButton" Content="←" Width="34" Margin="0,0,10,0" DockPanel.Dock="Left" ToolTip="На главную" AutomationProperties.Name="На главную"/>
                <TextBlock Text="Экспорт анимаций" Style="{StaticResource SectionTitle}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
            </DockPanel>
        </Border>
        <Grid x:Name="sheetWorkspaceGrid" Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="0"/></Grid.RowDefinitions>
            <Border x:Name="sheetSettingsPanel" Background="{StaticResource RaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="sheetOptions">
                        <TextBlock Text="Настройки" Style="{StaticResource CardTitle}" Margin="0,0,0,14"/>
                        <TextBlock Text="Раскладка" Margin="0,0,0,6"/>
                        <ComboBox x:Name="sheetLayout" SelectedIndex="0" Margin="0,0,0,14">
                            <ComboBoxItem Content="Горизонтально"/><ComboBoxItem Content="Вертикально"/><ComboBoxItem Content="Сетка"/>
                        </ComboBox>
                        <StackPanel x:Name="sheetColumnsPanel" Visibility="Collapsed">
                            <TextBlock Text="Столбцы" Margin="0,0,0,6"/>
                            <local:IntegerInput x:Name="sheetColumns" Value="4" Minimum="1" Maximum="256" Margin="0,0,0,14"/>
                        </StackPanel>
                        <TextBlock Text="Промежуток, px" Margin="0,0,0,6"/>
                        <local:IntegerInput x:Name="sheetPadding" Value="0" Minimum="0" Maximum="128" Margin="0,0,0,14"/>
                        <TextBlock Text="Масштаб предпросмотра" Margin="0,0,0,6"/>
                        <ComboBox x:Name="sheetZoom" SelectedIndex="0" Margin="0,0,0,14">
                            <ComboBoxItem Content="Вписать"/><ComboBoxItem Content="50%"/><ComboBoxItem Content="100%"/><ComboBoxItem Content="200%"/><ComboBoxItem Content="400%"/><ComboBoxItem Content="800%"/>
                        </ComboBox>
                        <TextBlock Text="Фон предпросмотра" Margin="0,0,0,6"/>
                        <ComboBox x:Name="sheetBackground" SelectedIndex="0" Margin="0,0,0,16">
                            <ComboBoxItem Content="Шахматка"/><ComboBoxItem Content="Тёмный"/><ComboBoxItem Content="Светлый"/>
                        </ComboBox>
                        <Button x:Name="sheetClearButton" Content="Очистить результаты" IsEnabled="False"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            <Grid x:Name="sheetContentPanel" Grid.Column="1" Margin="12">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*" MinHeight="230"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <TextBlock Text="ПРЕДПРОСМОТР" Style="{StaticResource Caption}" Margin="0,0,0,8"/>
                <Border x:Name="sheetDropPanel" Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource ThumbnailBrush}">
                    <local:PixelPreview x:Name="sheetPreview" EmptyText="Перетащите сюда файлы .aseprite или .ase&#x0a;Спрайтлисты будут подготовлены автоматически"/>
                </Border>
                <TextBlock x:Name="sheetDetail" Grid.Row="2" Margin="0,8,0,8" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" MaxHeight="52"/>
                <StackPanel Grid.Row="3">
                    <TextBlock x:Name="sheetCount" Text="РЕЗУЛЬТАТЫ" Style="{StaticResource Caption}" Margin="0,0,0,8"/>
                    <ListBox x:Name="sheetStrip" Height="124" Background="Transparent" BorderThickness="0" Padding="0"
                             ItemContainerStyle="{StaticResource SheetItemStyle}" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.Name="Результаты экспорта анимаций">
                        <ListBox.Template><ControlTemplate TargetType="ListBox">
                            <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ScrollViewer" Focusable="False" CanContentScroll="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"><ItemsPresenter/></ScrollViewer>
                            </Border>
                        </ControlTemplate></ListBox.Template>
                        <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                        <ListBox.ItemTemplate><DataTemplate>
                            <Grid Width="132" ToolTip="{Binding Detail}">
                                <StackPanel>
                                    <Border Height="46" Background="{StaticResource ThumbnailBrush}"><Image Source="{Binding Thumbnail}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/></Border>
                                    <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis" FontSize="12" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding State}" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0"/>
                                </StackPanel>
                                <Button Style="{StaticResource RemoveThumbnailButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="RemoveSheet" ToolTip="Убрать из списка" AutomationProperties.Name="Убрать из списка"/>
                            </Grid>
                        </DataTemplate></ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>