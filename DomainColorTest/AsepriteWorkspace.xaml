<UserControl x:Class="DomainColorTest.AsepriteWorkspace"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DomainColorTest" Background="#101010" Foreground="#EEEEEE">
    <UserControl.Resources><ResourceDictionary>
        <ResourceDictionary.MergedDictionaries><ResourceDictionary Source="Theme.xaml"/></ResourceDictionary.MergedDictionaries>
        <Style x:Key="SheetItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,8,0"/><Setter Property="Padding" Value="5"/>
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
                <Border x:Name="Frame" Background="#171717" BorderBrush="#3A3A3A" BorderThickness="1" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsSelected" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="#EEEEEE"/><Setter TargetName="Frame" Property="Background" Value="#303030"/></Trigger>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="#AAAAAA"/></Trigger>
                    <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="Frame" Property="BorderBrush" Value="White"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </ResourceDictionary></UserControl.Resources>
    <Grid Margin="18,16,18,18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*" MinHeight="120"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,16">
            <Button x:Name="sheetBackButton" Content="←" Width="34" Height="32" DockPanel.Dock="Left" Margin="0,0,14,0" ToolTip="На главную" AutomationProperties.Name="На главную"/>
            <TextBlock Text="Экспорт анимаций" FontSize="25" FontWeight="SemiBold"/>
        </DockPanel>
        <StackPanel Grid.Row="1">
            <WrapPanel>
                <Button x:Name="sheetOpenButton" Content="Открыть файлы…" Padding="14,9" Margin="0,0,10,12" ToolTip="Открыть несколько .aseprite или .ase (Ctrl+O)"/>
                <Button x:Name="sheetSaveButton" Content="Сохранить выбранный…" Padding="14,9" Margin="0,0,10,12" IsEnabled="False"/>
                <Button x:Name="sheetSaveAllButton" Content="Сохранить все…" Style="{StaticResource PrimaryButton}" Padding="14,9" Margin="0,0,10,12" IsEnabled="False"/>
                <Button x:Name="sheetCancelButton" Content="Отменить" Padding="14,9" Margin="0,0,0,12" Visibility="Collapsed"/>
            </WrapPanel>
            <WrapPanel x:Name="sheetOptions">
                <StackPanel Margin="0,0,16,12"><TextBlock Text="Раскладка" Margin="0,0,0,5"/><ComboBox x:Name="sheetLayout" Width="160" SelectedIndex="0"><ComboBoxItem Content="Горизонтально"/><ComboBoxItem Content="Вертикально"/><ComboBoxItem Content="Сетка"/></ComboBox></StackPanel>
                <StackPanel x:Name="sheetColumnsPanel" Margin="0,0,16,12" Visibility="Collapsed"><TextBlock Text="Столбцы" Margin="0,0,0,5"/><local:IntegerInput x:Name="sheetColumns" Value="4" Minimum="1" Maximum="256" Width="82"/></StackPanel>
                <StackPanel Margin="0,0,16,12"><TextBlock Text="Промежуток, px" Margin="0,0,0,5"/><local:IntegerInput x:Name="sheetPadding" Value="0" Minimum="0" Maximum="128" Width="105"/></StackPanel>
                <StackPanel Margin="0,0,16,12"><TextBlock Text="Масштаб" Margin="0,0,0,5"/><ComboBox x:Name="sheetZoom" Width="108" SelectedIndex="0"><ComboBoxItem Content="Вписать"/><ComboBoxItem Content="50%"/><ComboBoxItem Content="100%"/><ComboBoxItem Content="200%"/><ComboBoxItem Content="400%"/><ComboBoxItem Content="800%"/></ComboBox></StackPanel>
                <StackPanel Margin="0,0,0,12"><TextBlock Text="Фон" Margin="0,0,0,5"/><ComboBox x:Name="sheetBackground" Width="120" SelectedIndex="0"><ComboBoxItem Content="Шахматка"/><ComboBoxItem Content="Тёмный"/><ComboBoxItem Content="Светлый"/></ComboBox></StackPanel>
            </WrapPanel>
        </StackPanel>
        <Border x:Name="sheetDropPanel" Grid.Row="2" BorderBrush="#3A3A3A" BorderThickness="1">
            <local:PixelPreview x:Name="sheetPreview" EmptyText="Перетащите сюда файлы .aseprite или .ase&#x0a;Спрайтлисты будут подготовлены автоматически"/>
        </Border>
        <TextBlock x:Name="sheetDetail" Grid.Row="3" Margin="0,10,0,10" Foreground="#BDBDBD" TextWrapping="Wrap" MaxHeight="52"/>
        <StackPanel Grid.Row="4">
            <DockPanel Margin="0,0,0,8">
                <Button x:Name="sheetClearButton" Content="Очистить" DockPanel.Dock="Right" IsEnabled="False"/>
                <TextBlock x:Name="sheetCount" Text="Результаты (0)"/>
            </DockPanel>
            <ListBox x:Name="sheetStrip" Height="124" Background="Transparent" BorderThickness="0" Padding="0"
                     ItemContainerStyle="{StaticResource SheetItemStyle}" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                     ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.Name="Результаты экспорта анимаций">
                <ListBox.Template><ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ScrollViewer" Focusable="False" CanContentScroll="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"><ItemsPresenter/></ScrollViewer>
                    </Border>
                </ControlTemplate></ListBox.Template>
                <ListBox.ItemsPanel><ItemsPanelTemplate><VirtualizingStackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                <ListBox.ItemTemplate><DataTemplate>
                    <Grid Width="132" ToolTip="{Binding Detail}">
                        <StackPanel>
                            <Border Height="46" Background="#0D0D0D"><Image Source="{Binding Thumbnail}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/></Border>
                            <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis" FontSize="12" Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding State}" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="#AAAAAA" Margin="0,3,0,0"/>
                        </StackPanel>
                        <Button Style="{StaticResource RemoveThumbnailButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="RemoveSheet" ToolTip="Убрать из списка" AutomationProperties.Name="Убрать из списка"/>
                    </Grid>
                </DataTemplate></ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
